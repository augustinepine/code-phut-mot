import React from "react";

const LiquidText: React.FC = () => {
  return (
    <h1
      className="!text-[6rem] tracking-[0.3em] font-extrabold relative text-transparent 
    bg-clip-text bg-gradient-to-r from-cyan-400 bg-[length:200%_200%] 
    via-blue-500 to-red-400 animate-wave"
    >
      LIQUID
    </h1>
  );
};

export default LiquidText;
